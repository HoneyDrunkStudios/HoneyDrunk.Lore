---
source: "https://developer.blender.org/docs/release_notes/5.2/cycles/"
title: "Cycles - Blender Developer Documentation"
author: "Blender Developers"
date_published: "unknown"
date_clipped: "2026-06-12"
category: "Technical Art & Creator Tools"
source_type: "web"
---

# Blender 5.2 LTS: Cycles[¶](#blender-52-lts-cycles "Permanent link")

## Texture Cache[¶](#texture-cache "Permanent link")

On scenes with many image textures, the texture cache significantly reduces memory usage and startup time. This comes at the cost of a small rendering performance impact, and increase disk space usage. ([PR#154913](https://projects.blender.org/blender/blender/pulls/154913), [PR#157244](https://projects.blender.org/blender/blender/pulls/157244))

To get started, enable Performance > Texture Cache and Auto Generate in the render properties. For every image, a corresponding tx file will be generated in the `blender_tx/` folder next to the image file. This tx file optimized for loading just the tiles and resolutions needed for rendering. It is automatically updated when the image file is modified.

### Performance[¶](#performance "Permanent link")

Memory savings and performance are highly scene dependent. Especially how much image texture memory was used compared to geometry. The following numbers are from benchmark scenes that use many images.

Tile Size under Performance > Memory can be tweaked to further reduce memory usage. For each render tile only the required image textures are loaded.

### Generating Tx Files[¶](#generating-tx-files "Permanent link")

The Texture Cache panel has a button to manually generate tx files. The default location can be changed in the preferences, with either a path relative to the image file, or an absolute path.

Standard tx files generated by other renderers and OpenImageIO's `maketx` are recognized as well, but may be less efficient to load. To generate Cycles native tx files from the command line, the following commands as provided:

```
# Generate tx files for all images in a blend file.
blender scene.blend --command maketx
# Generate tx file for a specific image file. This requires specifying the
# color space if it can not be determined automatically.
blender --command maketx image.png --colorspace sRGB
```

### Simplify[¶](#simplify "Permanent link")

In the Simplify panel, there is a new Texture Resolution percentage setting, in addition to the existing Texture Limit.

This is particularly useful for viewport rendering of complex scenes with the texture cache. Textures at e.g. 50% or 25% resolution will reduce load times and can help ensure textures fit fully in memory for interactive rendering.

### Open Shading Language[¶](#open-shading-language "Permanent link")

The texture cache is also used for Open Shading Language, which previously used OpenImageIO. This means improved performance and GPU support. However some more advanced options for `texture()` and `gettextureinfo()` are no longer available.

## Raycast Attributes[¶](#raycast-attributes "Permanent link")

The Raycast node now provides access to attributes at the intersection point ([PR#157344](https://projects.blender.org/blender/blender/pulls/157344)).

Add attributes in the Sample Attributes panel in the node editor sidebar, and new attribute sockets appear on the node. Fill in the attribute name and link the new output socket to use the value. When no intersection is found, the attribute output is zero.

Support is currently limited to Cycles. When using EEVEE, a warning is shown on the node and the output is zero.

## Shader Nodes[¶](#shader-nodes "Permanent link")

### Subsurface Scattering[¶](#subsurface-scattering "Permanent link")

- Subsurface BSDF and Subsurface component in Principled BSDF now supports negative anisotropy. ([PR#156355](https://projects.blender.org/blender/blender/pulls/156355))

  Subsurface Anisotropy from -1 to 1

- The Random Walk radius scaling ([PR#133848](https://projects.blender.org/blender/blender/pulls/133848)) and anisotropy to albedo mapping ([PR#156355](https://projects.blender.org/blender/blender/pulls/156355)) have been adjusted to match other renderers better. To keep compatibility, older files are automatically converted to use Random Walk (Legacy) mode.

## Other features[¶](#other-features "Permanent link")

- Add Cast Shadows options to world. ([PR#156586](https://projects.blender.org/blender/blender/pulls/156586))
- Reduce geometry memory usage and improve sync performance by sharing some memory with Blender. ([PR#155970](https://projects.blender.org/blender/blender/pulls/155970), [PR#158728](https://projects.blender.org/blender/blender/pulls/158728))

Back to top
