# Graph Index Placeholder

Lore is markdown-first today. This directory is reserved for generated typed-entity and relationship indexes once the wiki grows past the flat-file threshold.

Until graph tooling exists, ingest/compile passes should keep pages graph-ready by writing typed entities and explicit relationship statements in markdown.